@model IEnumerable<Notification>

<h2>All Advice Provided by Financial Advisors</h2>

@if (Model != null && Model.Any())
{
    <table class="table">
        <thead>
            <tr>
                <th>User Email</th>
                <th>Date</th>
                <th>Advice</th>
                <th>Status</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var advice in Model)
            {
                <tr>
                    <td>@advice.UserEmail</td>
                    <td>@advice.NotificationDate.ToShortDateString()</td>
                    <td>@advice.Message</td>
                    <td>@(advice.IsRead ? "Read" : "Unread")</td>
                </tr>
            }
        </tbody>
    </table>
}
else
{
    <p>No advice available at the moment.</p>
}

